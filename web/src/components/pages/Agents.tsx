import Placeholder from './Placeholder'

export default function Agents() {
  return (
    <Placeholder
      title="Agents"
      blurb="Data-driven agent definitions (persona, model, toolkits) — manage the agents that answer in the extension."
    />
  )
}
